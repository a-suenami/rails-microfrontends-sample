<%= form_with(model: product_service_product, local: true) do |form| %>
  <% if product_service_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_service_product.errors.count, "error") %> prohibited this product_service_product from being saved:</h2>

      <ul>
      <% product_service_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
